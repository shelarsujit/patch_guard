# Patch-Guard — pinned dependencies.
# Verified on CPython 3.12 (devcontainer, canonical) and 3.14.5 (dev host).
#
# langgraph is floored at 1.0.10: CVE-2026-28277 is an unsafe msgpack
# checkpoint-deserialization flaw fixed in that release. This repo is public,
# so a vulnerable checkpointer must not ship.

langgraph==1.2.11
langgraph-checkpoint==4.2.0
langchain-core==1.6.1

litellm==1.98.0
mini-swe-agent==2.4.6
mcp==2.1.1

pydantic==2.13.5
jinja2==3.1.6
python-dotenv==1.2.3
pytest==9.1.1
