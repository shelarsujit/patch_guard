# --- Hackathon reference docs (micro1's, not ours to redistribute) ---
Problem_Statement.md
hackathon_rules_and_info.docx
*.pdf

# --- Secrets ---
.env
.env.local
*.key

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# --- Run artifacts (regenerated by `make eval`) ---
.scratch/
*.log

# --- OS / editor ---
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/settings.json

# Trajectories produced by the test suite (real runs are committed)
trajectories/test/
trajectories/trajtest/
results/test*.jsonl
trajectories/smoke*/
results/_smoke*
video/build/

# The video generator itself. The rendered mp4 ships; the pipeline that built it
# is not part of Patch-Guard and would read as scaffolding in a review. Kept on
# disk so the video stays rebuildable, and in history at c4eb498.
video/build_video.py
video/sections.py
video/__init__.py
docs/VIDEO_SCRIPT.md
